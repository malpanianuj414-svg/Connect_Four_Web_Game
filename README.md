A Web game built using HTML, CSS and JavaScript with a offline mode two players sitting next to each other playing Connect 4 Game. 
Used the CSS Grid and flexbox properties to keep the layout clean and responsive across different viewports.
